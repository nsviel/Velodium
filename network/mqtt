sudo apt install mosquitto mosquitto-clients


Test:
-----------
-brocker
    sudo mosquitto

-if brocker already running
    ps -ef | grep mosquitto
    sudo kill 12345

-suscriber 
    mosquitto_sub -h 127.0.0.1 -t Obstacle


Connection:
-----------
    - Broker adress (Protocol / IP / port) => "tcp://localhost:1883"
    - Topic label => "Obstacle"
    - Client ID => "AI_module"


Infos to provide:
-----------
    - Dimension
    - Most similar label
    - Localization


