#ifndef SCALA_H
#define SCALA_H

#include "../../common.h"


class Scala
{
public:
  //Constructor / Destructor
  Scala();
  ~Scala();

public:
  void loading();
  void reorganize_data(vector<Cloud*> clouds);

private:


};

#endif
